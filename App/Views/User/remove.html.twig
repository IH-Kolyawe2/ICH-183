{% extends '/layout/base.html.twig' %}

{% block title %}
    Edition d'un utilisateur
{% endblock %}

{% block content %}

    <section id="authentication" class="bg-light">
        <div class="container">
            <div class="row">
                <div class="col-lg-8 mx-auto">
                    <h2>Supression d'un utilisateur</h2>
                    <form method="POST">
                        <div class="form-group">
                            <label for="idUser">ID</label>
                            <input type="text" class="form-control" id="idUser" name="idUser" readonly="readonly" value="{{ user.idUser }}">
                        </div>
                        <div class="form-group">
                            <label for="firstname">Prénom</label>
                            <input type="text" class="form-control" id="firstname" name="firstname" readonly="readonly" placeholder="Prénom" value="{{ user.firstname }}">
                        </div>
                        <div class="form-group">
                            <label for="lastname">Nom</label>
                            <input type="text" class="form-control" id="lastname" name="lastname" readonly="readonly" placeholder="Nom" value="{{ user.lastname }}">
                        </div>
                        <div class="form-group">
                            <label for="mailAddress">Adresse email</label>
                            <input type="email" class="form-control" id="mailAddress" name="mailAddress" readonly="readonly" placeholder="Adresse email" value="{{ user.mailAddress }}">
                        </div>
                        <div class="form-group">
                            <input type="hidden" name="{{ csrf.key }}" value="{{ csrf.value }}">
                            <a href="/user/index" class="btn btn-secondary">Annuler</a>
                            <button type="submit" class="btn btn-danger">Supprimer</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </section>

{% endblock %}